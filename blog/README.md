# Blog posts

Write posts here as Markdown files, using lowercase filenames with hyphens.

## Posts

- [Supervised Learning vs. Unsupervised Learning](supervised-vs-unsupervised-learning.html) — website article
- [Markdown source](supervised-vs-unsupervised-learning.md)

The website serves the HTML article directly and links to it from the Blog view in `index.html`. Article styles live in `article.css`. No build step is required.

Keep the Markdown source and HTML article in sync when editing. To publish another post, add its Markdown source and HTML page here, then add a post card to the Blog view in `index.html`.
